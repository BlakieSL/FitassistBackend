spring:
  threads:
    virtual:
      enabled: true

  data:
    redis:
      host: localhost
      port: 6379

  datasource:
    url: jdbc:mysql://localhost:3306/test-db
    username: test
    password: test

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    open-in-view: false

  cloud:
    aws:
      s3:
        enabled: false
      ses:
        enabled: false
      credentials:
        access-key: test
        secret-key: test
      region:
        static: eu-central-1

redis-flag:
  enabled: false

s3:
  bucket:
    name: test-bucket

lucene:
  enabled: false

jws:
  sharedKey: adskjfa;sdkfjasd;asdfasdfasdfdasfasdgeawtrewt

rate-limiter:
  user-rate: 10000
  user-interval: 1
  key-rate: 10000
  key-interval: 1

app:
  frontend:
    url: http://localhost:5173
  email:
    from: test@example.com

logging:
  level:
    root: INFO
